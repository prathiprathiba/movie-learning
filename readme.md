# movie areas app
provide a movie api for React js app 

# important commands
`1. npm init`
`2. npm run server`
`3. git init`
`4. git add.`
`5. git commit -m "your msg"`
`6. git push -u orgin main`